// src/main.tsx
import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>Message Handler</h1>
  </React.StrictMode>,
  document.getElementById('root'), // Ensure you have a corresponding HTML element with this ID
);
